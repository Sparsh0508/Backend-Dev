
UNIX, GIT & GITHUB COMMANDS
==========================

--------------------------------------------------
MODULE 1: UNIX BASIC COMMANDS
--------------------------------------------------

1. pwd
Command:
pwd
Output:
/home/user/projects

2. ls
Command:
ls
Output:
Documents  Downloads  file.txt

3. ls -l
Command:
ls -l
Output:
-rw-r--r-- 1 user user  1024 file.txt

4. mkdir test
Command:
mkdir test
Output:
(No output)

5. cd test
Command:
cd test
Output:
(No output)

6. touch demo.txt
Command:
touch demo.txt
Output:
(No output)

7. rm demo.txt
Command:
rm demo.txt
Output:
(No output)

8. cat file.txt
Command:
cat file.txt
Output:
Hello World

9. head file.txt
Command:
head file.txt
Output:
Hello World

10. tail file.txt
Command:
tail file.txt
Output:
Hello World

11. grep "Hello" file.txt
Command:
grep "Hello" file.txt
Output:
Hello World

12. chmod 755 script.sh
Command:
chmod 755 script.sh
Output:
(No output)

--------------------------------------------------
MODULE 2: GIT BASIC COMMANDS
--------------------------------------------------

1. git init
Command:
git init
Output:
Initialized empty Git repository

2. git status
Command:
git status
Output:
On branch main
nothing to commit, working tree clean

3. git add .
Command:
git add .
Output:
(No output)

4. git commit -m "Initial commit"
Command:
git commit -m "Initial commit"
Output:
[main abc123] Initial commit
 1 file changed

5. git log --oneline
Command:
git log --oneline
Output:
abc123 Initial commit

--------------------------------------------------
MODULE 3: BRANCHING
--------------------------------------------------

1. git branch feature
Command:
git branch feature
Output:
(No output)

2. git checkout feature
Command:
git checkout feature
Output:
Switched to branch 'feature'

3. git merge feature
Command:
git merge feature
Output:
Already up to date.

--------------------------------------------------
MODULE 4: REMOTE REPOSITORY
--------------------------------------------------

1. git remote -v
Command:
git remote -v
Output:
origin  https://github.com/user/repo.git (fetch)
origin  https://github.com/user/repo.git (push)

2. git push origin main
Command:
git push origin main
Output:
Everything up-to-date

--------------------------------------------------
END OF FILE
--------------------------------------------------
